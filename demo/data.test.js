import {test} from 'node:test';import assert from 'node:assert/strict';import {payload,series} from './data.js';import {createApp} from './server.js';
test('generated series are repeatable and populated',()=>{assert.deepEqual(series('DGS10'),series('DGS10'));assert.equal(series().observations.length,960);assert.notDeepEqual(series('DGS10'),series('SP500'));});
test('demo provides synthetic data and removes report endpoints',async()=>{const server=createApp().listen(0,'127.0.0.1');await new Promise(r=>server.once('listening',r));try{const base=`http://127.0.0.1:${server.address().port}`;assert.equal((await fetch(base+'/api/report/blocks')).status,404);const r=await fetch(base+'/api/graph/data');assert.equal((await r.json()).graph.nodes.length,5);assert.match(r.headers.get('content-security-policy'),/connect-src 'self'/);}finally{server.close();}});
